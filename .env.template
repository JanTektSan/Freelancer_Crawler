# MongoDB Configuration
# MongoDB connection URL - Change if using remote MongoDB or different port
MONGODB_URL=mongodb://localhost:27017

# Database name for storing user information cache
DATABASE_NAME=user_crawler

# Optional: Uncomment and modify if you want to override hardcoded values in main.py
# Note: These variables are not currently used in main.py but could be added for customization

# Server Configuration (currently hardcoded in main.py)
# HOST=0.0.0.0
# PORT=8000

# API Configuration (currently hardcoded in main.py)
# FREELANCER_API_BASE=https://www.freelancer.com/api/users/0.1/users
# REQUEST_TIMEOUT_SECONDS=30
# MAX_USER_IDS_PER_REQUEST=100
# QUEUE_MAX_SIZE=1000
# RETRY_DELAY_SECONDS=5
# MAX_WAIT_RETRIES=10
# WAIT_RETRY_DELAY_SECONDS=1.0

# Logging Configuration (currently hardcoded in main.py)
# LOG_LEVEL=INFO
